services:
  backend:
    build:
      target: dev
    env_file:
      - .env
    volumes:
      - ./backend:/app

  frontend:
    build:
      target: dev
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
