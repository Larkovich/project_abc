services:
  backend:
    build:
      target: dev
    volumes:
      - ./backend:/app

  frontend:
    build:
      target: dev
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
